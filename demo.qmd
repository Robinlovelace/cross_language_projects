
::: {.panel-tabset group="language"}

## R

```{r}
#| echo: true
library(sf)
sf::write_sf(spData::world, "world.gpkg")
plot(spData::world)
```

## Python

```{python}
#| echo: true
import geopandas as gpd
world = gpd.read_file('world.gpkg')
world.plot()
```

## Julia

```{r}
write(Sys.getenv("LD_LIBRARY_PATH"), stderr())
Sys.setenv(LD_LIBRARY_PATH = "")
Sys.setenv(JULIA_PROJECT = ".")
library(JuliaCall)
install_julia()
```

```{julia}
using GeoDataFrames
using Plots
world = GeoDataFrames.read("world.gpkg")
# using CairoMakie, GeoMakie
# f, a, p = poly(world.geom)
# poly(f[1, 2], world.geom; axis=(; type=GeoAxis,))
plot(world.geom)
```

:::

::: {.panel-tabset group="language"}

## R

```{r}
# Test 1
```

## Python

```{python}
# Test 2
```

:::
